{
  "name": "chess-classroom",
  "version": "1.0.0",
  "private": true,
  "description": "Two-tab chess demonstration tool for teachers (static, zero-build-step app). package.json exists only to vendor third-party browser libraries and to run node's built-in test runner during development — see CLAUDE.md.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "serve": "python3 -m http.server 8000",
    "vendor": "node scripts/vendor-libs.mjs"
  },
  "devDependencies": {
    "chess.js": "1.4.0",
    "@mliebelt/pgn-parser": "1.4.19",
    "cm-chessboard": "8.12.19"
  }
}
